# 改造

0. 模型的运行态和配置态分离
比如Subscription在config和pipeline里都存在一样的名字，类似的pipeline也是存在一样的问题

1. 引入namespace的概念做顶层的隔离，类似K8S的设计方案。

2. pipeline的labels需要加索引，去掉pipelineLabels的概念。

3. Alert和Evidence设计需要考虑调整。
告警需要收敛，但是每次告警的evidence需要保留关联，也就是一个alert对应多个evidence或者叫execution。帮我考虑下设计
收敛的窗口需要注意，当前是使用fingerprint做幂等的，引入收敛概念后需要调整 可以需要结合时间信息来收敛到一个fingerprint？但是任然存储evidence或者execution记录。

4. Event 模型抽象与隔离
当前所有来源都使用一个Event，但是其实出了CDC来源 其他的不会严格校验db，table，op，before，after等字段。每个source的事件模型有些出入。

5. CronSource改造
当前的设计没办法支持特定时间点执行特定pipeline的功能。需要改成支持类似cron表达式的方式

6. 清理掉demo的代码

